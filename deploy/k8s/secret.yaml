# Placeholder Secret. In production, do NOT commit real values here —
# source these from AWS Secrets Manager (e.g. via the External Secrets Operator,
# which syncs Secrets Manager into a k8s Secret of this name).
apiVersion: v1
kind: Secret
metadata:
  name: pw-ims-service-secrets
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
